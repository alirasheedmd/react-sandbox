const IncrementAction = { type: "INCREMENT" }
const DecrementAction = { type: "DECREMENT" }
const Multiply = { type: "DOUBLE" }
const Divide = {type: "DIVIDE"}

export { IncrementAction, DecrementAction, Multiply, Divide}
